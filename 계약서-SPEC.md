# Forge Kitchen Ops — 프로젝트 계약서

> **사용법:** 이 파일을 새 프로젝트 폴더에 `계약서.md`라는 이름으로 복사해 넣는다.
> 에이전트는 이 문서를 최우선 지침으로 삼고, 마일스톤마다 다시 읽는다.

---

너는 시니어 풀스택 엔지니어이자 시스템 설계자다. 이번 대화의 목표는 Amazon FBA 제품 리서치·소싱 자동화 시스템 "Forge Kitchen Ops"를 완전히 새로 만드는 것이다. 아래는 바뀌지 않는 계약이며, 이를 위반하는 설계는 거부된다.

## 0. 이 시스템을 쓰는 사람
- Jay: 주방용품 Amazon FBA 판매자(브랜드: Forge Kitchen). 개발자가 아니라 운영자다. 코드, 터미널, 서버 개념을 화면에 노출하지 마라.
- 기준값: 첫 출시 예산 $3,000 / 광고 전 마진 목표 35% / 광고 후 마진 목표 35% / ROI 150%. 이 값들은 관리자 설정에서 언제든 바꾸는 "현재 설정"이며, 시스템·AI·후보 입력이 임의로 낮추거나 완화할 수 없다.
- UI 기본 언어는 한국어(영어 전환 지원). 모든 수치는 [측정/추정/견적/미확인] 중 무엇인지 항상 함께 표시한다. "미확인"을 0이나 합격으로 대체하는 코드를 절대 허용하지 않는다.

## 1. 확정 요구사항 10가지
각 항목에 합격 기준을 붙인다. 플랜에서 이 기준을 그대로 검증 포인트로 써라.

1) 아마존 리서치 봇 (핵심 파이프라인)
- CSV 업로드 → 니치 1차 필터 → Jungle Scout API 검증 → 소싱 → 마진 평가 → 발주 판단 자료까지 하나의 파이프라인으로 연결.
- 합격 기준: 서로 다른 키워드 20행짜리 CSV 하나가 한 번의 업로드로 20개 후보가 되고, 각 후보가 [현재 단계 / 근거 / 모르는 것 / 다음 행동 1개]를 가진 채 화면에 보인다.

2) Jungle Scout 웹 리서치 + CSV 추출
- Jay가 로그인한 실제 브라우저 세션(브라우저 자동화 에이전트 또는 수동 다운로드)에서 Opportunity Finder 검색 → CSV 다운로드 → 임포트까지 최소 클릭으로 연결. 검색 필터(카테고리·가격·수요·경쟁·계절성)는 저장해서 재사용.
- 임포트는 원본 파일, 행 번호, source hash를 보존한다. 다른 화면의 표를 Opportunity Finder 형식으로 위장해 올리지 않는다.
- 웹 세션이 없으면 해당 후보는 "웹 확인 대기" 상태로 명시하고, 세션이 살아나면 이어서 진행한다.
- 합격 기준: 실제 검색 → CSV → 임포트가 UI에서 3클릭 이내, 출처 추적 가능.

3) Jungle Scout 공식 API 검증
- Product Database / Keywords / Historical Search Volume / Sales Estimates / Share of Voice 등 공식 endpoint만 사용.
- 일일 호출 예산은 DB에서 원자적으로 승인하고 attempt 이력을 남긴다. 캐시 키에 endpoint·marketplace·정규화 쿼리·기간·필터·버전을 포함. 논리 작업 수와 실제 wire request 수를 분리 표시. 429/5xx 재시도는 예산을 우회하지 않는다.
- 합격 기준: 예산 소진 시 추가 호출 0 / 워커 재시작 후 중복 유료 호출 0 / 캐시 히트 시 wire call 0.

4) 마진·경쟁 평가 + 알리바바 소싱
- 검증 통과 후보 → 동일 사양 공급처 후보 수집(Alibaba 등) → 견적 요청 초안 작성 → Jay 승인 후 전송 → 회신 기록 → 조건 비교.
- 손익 공식: 단위 기여이익 = 판매가 − 도착원가 − Amazon 수수료(FBA/referral) − 광고 − 반품/손실 예상 − 기타 변동비. 출시 현금 = 초도 도착원가 + 별도 선지급 비용 + 초기 광고 현금 + 예비비. 목표 마진·ROI 대비 GO / CAUTION / 보류 판정.
- 견적 없음은 탈락이 아니다. 서로 다른 견적의 최저 단가와 최저 MOQ를 조합하지 않는다.
- 합격 기준: 견적 2건 입력 → 자동 손익·현금 계산 → 기준 대비 판정이 화면에 재현.

5) Oracle Cloud 24/7 배포
- 큐 워커는 Jay의 Oracle Cloud VM에서 systemd 서비스(+스케줄 타이머)로 상시 실행. 서버 재부팅 시 자동 기동, 프로세스 사망 시 자동 복구, 재시작 시 큐 잔여 작업을 중복 없이 재개.
- 프로덕션 권한은 단 하나: "프로덕션 워커는 오직 Oracle에서만"을 문서와 기동 스크립트 양쪽에서 강제. 개발 머신의 워커가 프로덕션 DB의 작업을 소비하는 구조를 만들지 않는다.
- 합격 기준: 재부팅/프로세스 kill 테스트로 자동 복구를 실측.

6) UI/UX — "누가 봐도 이렇게 쓰는 것"
- 모든 화면의 문법: 이 후보는 지금 어느 단계 + 근거는 뭔지 + 뭘 모르는지 + 다음 행동 1개. 대시보드 첫 화면은 "오늘 내가 승인할 것".
- 사람의 개입이 필요한 승인 포인트(연락 전송, 발주 검토, 예산 변경)만 크고 명확한 버튼으로. 나머지는 자동이다.
- 코드 작성 전에 UI 디자인 시스템(색·타이포·레이아웃·화면별 문법)을 문서로 만들어 승인받고, 그 시스템으로 전체를 구현한다. 페이지별 임시 스타일 금지.
- 375/768/1280 반응형, WCAG AA 준수.
- 합격 기준: 개발 지식 없는 제3자가 설명서 없이 후보 1개를 발굴부터 견적 대기까지 진행.

7) ChatGPT·Grok 구독 OAuth (조건부, P1)
- 공식 OAuth/구독 연동을 제공하는 프로바이더만 지원. 약관·권한 근거가 확인될 때까지 해당 어댑터는 fail-closed(비활성, 라우팅 불가)로 만든다.
- 구독 어댑터는 격리된 플러그인 구조로: 실패하거나 비활성이어도 코어 파이프라인에 영향이 0이어야 한다. 약관 위반 방식(세션 탈취, 비공식 클라이언트 위장)은 금지.
- 합격 기준: 미인가 구독 어댑터가 유료·외부 호출을 하나도 못 한다는 것을 테스트로 증명.

8) 커스텀 AI 프로바이더
- API를 제공하는 AI 프로바이더(OpenAI 호환 등)를 화면에서 등록/수정/역할 지정/우선순위/테스트/비활성 가능.
- 키는 write-only(입력 후 마지막 4자리만 표시), 서버에서 암호화 저장, 로그·에러·응답 본문에 시크릿 노출 금지.
- 요청 URL은 HTTPS 고정 + 사설 IP/루프백/클라우드 메타데이터 주소 차단(SSRF 방지).
- 합격 기준: 프로바이더 2개 등록 → 역할 분기 동작 → 테스트 성공, 키 유출 경로 0.

9) 자동화 (무인 운영)
- 데일리 플래너가 예산 내에서 "다음에 할 일"을 자동 선정·실행. 이메일 알림으로 진전·탈락 사유·승인 대기 요약을 전달.
- 사람 개입 포인트는 4개로 한정: 업체 연락 전송 승인 / 발주 판단 / 예산·기준 변경 / 신규 프로바이더 활성화. 이 네 개만 제외하면 전부 무인.
- 합격 기준: 하루 스케줄 실행에서 사람 조작 0으로 후보들이 단계 전진 + 아침 요약 알림 수신.

10) 보안 (유출 0)
- 관리자 인증: 비밀번호 + TOTP 2FA, 로그인 실패 잠금. 신뢰한 기기 기억.
- 시크릿 암호화 저장, 감사 로그(누가 언제 무엇을 승인했는지), DB 자동 백업.
- 서버 접근은 VPN/Tailscale 권장, 관리 화면 origin 화이트리스트.
- 합격 기준: 보안 체크리스트 전 항목을 실측으로 통과.

## 2. 리서치 비즈니스 규칙 (그대로 박제)
- 니치 조건(5항목 중 4개 통과): ① 리뷰 장벽 — 700+ 리뷰 상품 ≤3개, 2000+ 리뷰 ≥2개면 탈락 ② 1위 가격 $17–80 ③ 월 매출 $8,000+ 경쟁 상품 ≥5개 ④ Standard 크기 ⑤ 차별화 경로 존재.
- 추가 확인: 1위 점유율 <35%, 상위3 점유율 <55%, 첫 페이지 총매출 $350K+, 저리뷰 상품이 파는 기회, Amazon 자체 브랜드 출시/변형 과다/반품률/gated 카테고리 위험.
- 이 조건들은 설정으로 저장·표시하되 초기값은 위 숫자다. 지원하지 않는 항목은 0이 아니라 "미확인"으로 처리.
- 근거 계약: 모든 판단 입력은 {값, 분류: measured/estimate/quote/unknown, 출처, 시점}을 가진다. 근거 존재만으로 수익성 "확인" 처리 금지.
- 점수는 탐색 정렬용일 뿐 합격 기준이 아니다. 임의의 "성공 확률 %" 숫자를 생성하지 않는다.

## 3. 이전 버전에서 실패로 배운 12가지 — 이번엔 설계 단계에서 해결해라
1. 배포 권한 혼선: 개발 머신 워커와 서버 워커가 같은 큐를 잡는 사고가 실제로 일어났다 → "프로덕션은 어디서만 도는가"를 코드·문서·기동 스크립트로 강제.
2. 구독 OAuth가 코어 진행을 수 주간 막았다 → 어댑터 격리 + fail-closed로 코어와 분리.
3. 프론트 리디자인을 4번 반복하고도 최종 승인을 못 받았다 → 디자인 시스템 문서를 첫 산출물로 만들고 승인 후 구현.
4. 설정값이 스키마에만 존재하고 화면/API에서 편집 불가한 사태 → 설정은 "화면에서 실제로 바뀌고 다음 실행에 적용됨"까지를 검증 항목으로.
5. 실제로는 후보 1개만 돌아가는 병목을 6주 뒤에 발견 → 다중 후보 실측(요구 1의 합격 기준)을 첫 마일스톤에서 확인.
6. 같은 기능을 8라운드나 리뷰하는 무한 루프가 있었다 → 마일스톤마다 합격 기준을 미리 합의하고, 충족하면 다음으로 진행. 완벽주의 리팩터링 금지.
7. "미확인을 0으로 대체"하는 유혹은 코드 여러 군데서 발생 → 타입 차원에서 unknown을 표현해 0으로 쓸 수 없는 구조로 만들어라.
8. 중복 유료 호출 방지는 메모리 락이 아니라 DB 원자적 authorization + attempt 이력으로만 보장된다.
9. CSV/웹 데이터는 provenance(원본·행·hash·시점)가 없으면 나중에 신뢰할 수 없다 → 임포트 설계에 처음부터 포함.
10. 캐시 키에 버전이 없으면 스키마 변경 때 캐시 전체가 오염된다 → 캐시 키에 버전 포함.
11. DB 마이그레이션은 append-only로. 사용자 데이터 리셋은 금지.
12. 시크릿은 한 번 노출되면 회수 불가 → write-only 입력, last4 표시, 로그 차단을 프레임워크 레벨에서 강제.

## 4. 기술 고정 조건 / 네가 결정할 것
고정:
- 프로덕션 워커: Jay의 Oracle Cloud VM, systemd 상시 서비스.
- 웹(UI)은 Cloudflare에 배포한다. Workers/Pages 중 어느 쪽이 맞는지는 네가 근거 들어 결정해라.
- 모든 코드는 GitHub 저장소 https://github.com/ox8884/AmazonResearchbot 에서 진행한다. 저장소가 비어 있으면 초기 구조부터 올리고, 이후 의미 있는 단위로 커밋·푸시한다.
- DB: Postgres(마이그레이션 버전 관리 포함).
- 로컬 개발은 명령 한 번으로 웹+워커가 함께 실행.
- 풀스택 TypeScript 권장. 프레임워크 선택은 네가 근거와 함께 제안.
네가 결정해서 플랜에 제안할 것:
- 웹 프레임워크와 주요 라이브러리, 큐 구현 방식(Postgres 기반 권장), 브라우저 자동화 방식(요구 2), 알림 채널, 배포 파이프라인, 프로젝트 폴더 구조.

## 5. 기존 프로젝트 폴더 참고 규칙
- 이전 버전 폴더: C:\Users\hyj53\Documents\amazon research agent
- 이 폴더는 "사실 확인용 참고서"이지 설계 원본이 아니다. 요구사항·비즈니스 규칙·교훈은 전부 이 계약에 있으므로, 새 설계는 이 폴더를 보지 않고도 완결되어야 한다.
- 참고해도 되는 것: 외부 시스템의 실제 사실(정글스카웃 API 응답 형식·계정 한도, 오라클 서버 환경, Supabase/Tailscale 설정), 과거 장애·리뷰 기록, 마지막 마일스톤의 기존 데이터 이관.
- 참고해서는 안 되는 것: 이 폴더의 아키텍처·디자인·코드 구조를 기본값으로 가져오는 것. "원래 이렇게 만들었으니까"를 설계 근거로 쓰는 것.
- 폴더에서 가져온 사실은 어느 파일에서 확인했는지 함께 기록해라.

## 6. 진행 규칙
- 지금은 코드를 작성하지 마라. 첫 산출물은 플랜이다.
- 첫 응답은 순서대로: ① 네가 이해한 시스템 요약(10줄 내외) ② 모호해서 네가 정할 수 없는 결정 질문(최대 10개, 각각 네 기본안 포함) ③ 제안 아키텍처 + 마일스톤 플랜(마일스톤마다 산출물, 요구사항 합격 기준 매핑, 예상 규모).
- 필요한 정보가 있으면 질문하되, 답이 없으면 기다리지 말고 가장 안전한 기본안으로 진행하고 그 선택을 기록해라. 계약(요구사항·금지사항)과 충돌하는 기본안은 절대 선택하지 마라.
- 플랜 승인 후에는 마일스톤 순서대로 구현하고, 각 마일스톤 완료 시 실제 동작 증거(테스트 결과, 화면 동작 설명)를 보여라.
- 유료 API 호출, 외부 전송(이메일/업체 연락), 오라클·클라우드 프로덕션 배포, 데이터 삭제는 항상 사전 승인을 받아라.
- 커뮤니케이션은 한국어, 비개발자에게 설명하듯. 전문용어는 괄호로 풀어 써라.

### 무인 진행 규칙 (Jay가 자리를 비운 동안)
- 멈추고 기다리지 마라. 플랜 작성 → 구현 → 테스트 → GitHub 푸시를 계속 굴려라.
- 플랜은 프로젝트 루트의 PLAN.md에 기록하고, 승인 응답이 없어도 기본 플랜으로 진행해라. Jay가 확인 후 수정을 요청하면 반영한다.
- 질문은 프로젝트 루트의 QUESTIONS.md에 누적 기록해라: 날짜 / 질문 / 네 기본안 / 그 기본안으로 진행한 내용. Jay가 돌아오면 이 파일부터 답한다.
- 승인이 필요한 행위(유료 호출, 외부 전송, 프로덕션 배포, 데이터 삭제)는 실행하지 말고 "승인 대기"로 남기고, 막히지 않게 다른 작업으로 넘어가라.
- 계정·인증이 필요해서 진행이 불가능한 경우(GitHub은 이미 지정되어 있으므로 제외) 그 사실을 QUESTIONS.md에 남기고 가능한 다음 작업으로 이동해라.
- Jay가 돌아오면 첫 보고: 한 일 요약 / QUESTIONS.md 대기 질문 / 승인 대기 항목 / 다음 마일스톤 제안.

## 7. 최종 합격 시나리오 (이게 완료다)
"새 CSV 20행 업로드 → 밤새 무인으로 필터·API 검증·소싱 후보 수집 → 아침에 이메일 요약 수신 → 화면에서 견적 초안 2건 승인 → 승인된 후보가 견적 대기 단계로 이동"이 사람 개입 없이 재현되는 것. 모든 마일스톤은 이 시나리오를 향해 있다.
