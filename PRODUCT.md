# PRODUCT — Forge Kitchen Ops

운영자가 아침에 열어 “오늘 내가 결정할 일”만 처리하는 Amazon FBA 주방용품 리서치·소싱 도구다. 개발 화면이 아니다.

권위: `계약서-SPEC.md` 요구 1–10·비즈니스 규칙·최종 시나리오. 구현 세부는 `PLAN.md`. 화면 문법은 `DESIGN.md`.

## 사용자

Jay. Forge Kitchen 브랜드의 주방용품 Amazon 판매자. 개발자가 아니라 운영자. 코드, 터미널, 서버, SQL, raw URL, stack trace를 화면에 올리지 않는다.

한 사람만 로그인한다. 공개 가입 없음.

## 하는 일

한 파이프라인:

1. 저장 검색 또는 CSV로 키워드를 가져온다.
2. 서로 다른 키워드 20행이면 후보 20개.
3. 니치 규칙으로 확인된 탈락만 종료하고, 모르는 값은 0으로 바꾸지 않는다.
4. 공식 Jungle Scout API로 검증한다. 예산이 없으면 호출하지 않는다.
5. 통과 후보의 같은 사양으로 공급처를 모은다.
6. 견적 요청 초안을 만들고 Jay가 승인한 뒤에만 보낸다.
7. 견적 2건을 같은 필드로 비교하고 손익·출시 현금을 계산한다.
8. 발주 판단 자료와 결정만 기록한다. 결제·자동 구매는 없다.

각 후보는 화면에 항상 네 가지다: **지금 단계 / 근거 / 모르는 것 / 다음 행동 1개**.

## 고정 초기 기준

설정 화면에서 바꾸고 승인해야 다음 실행에 적용된다. 앱·API·AI가 임의로 완화하지 않는다.

| 키 | 초기값 |
|---|---|
| 시장 | Amazon US |
| 통화 | USD (번역하지 않음) |
| 출시 현금 한도 | 3000.00 |
| 광고 전 마진 | 35% |
| 광고 후 마진 | 35% |
| ROI | 150% (광고 후 단위 기여이익 ÷ 단위 도착원가 × 100) |
| 시간대 | Asia/Seoul |
| 아침 요약 | 08:00, 승인된 이메일 범위 안에서만 외부 발송 |

니치 5규칙 중 4통과. 리뷰 2000+ 상품이 2개 이상이면 다른 점수와 무관하게 탈락. 점수는 정렬용이며 “성공 확률 %”를 만들지 않는다.

모든 숫자는 측정 / 추정 / 견적 / 미확인. 미확인은 0도 합격도 아니다.

## 사람 개입 4가지

이 네 개만 큰 승인 버튼이다. 나머지는 자동이거나 기다림이다.

1. 업체 연락 전송
2. 발주 판단
3. 예산·기준 변경
4. 신규 프로바이더 활성화

로그인, 처음 CSV 선택, 계정 연결은 준비/복구이지 일상 무인 합격에 넣지 않는다.

## 최종 합격 장면

새 CSV 20행 업로드 → 밤새 무인으로 필터·API 검증·소싱 수집 → 아침 이메일 요약 → 화면에서 견적 초안 2건 승인 → 후보가 견적 대기로 이동.

## 화면 경로

| 경로 | 하는 일 |
|---|---|
| `/` | 오늘 처리할 승인 |
| `/research` | 저장 검색, CSV 가져오기 |
| `/candidates` | 후보 목록 |
| `/candidates/:id` | 근거·경쟁·소싱·계산 |
| `/sourcing` | 견적 요청·회신 비교 |
| `/settings` | 기준·예산·연결·언어·알림 |
| `/security` | 로그인 기기·복구·감사 |

언어 기본 한국어, 영어 전환. USD와 시장 이름은 번역하지 않는다.

## 기술 고정 (운영자에게 화면으로 보여 주지 않음)

- 웹: Cloudflare Workers Static Assets. 업무 응답 `Cache-Control: no-store`.
- 유일한 프로덕션 워커: Oracle VM systemd. 개발 워커가 프로덕션 일을 집어가지 않는다.
- DB: 새 Postgres `forge_ops`. 기존 Supabase를 붙이지 않는다.
- 로컬: 준비 후 `pnpm dev` 한 번.

## 공식 연동 — 가능 / 대기

| 연동 | 지금 | 근거 | 막힌 이유 / 다음 |
|---|---|---|---|
| Jungle Scout 저장 검색·CSV | 대기 | 로그인 웹이 필요 | 이 세션에 브라우저 제어 도구 없음. 쿠키 복사 안 함 |
| Jungle Scout 공식 API 5종 | 설계만, 호출 0 | OpenAPI·문서 확인 | 키·권한·일일 상한 미승인. cap 0 |
| Alibaba 공급처 수집·연락 | 대기 | 공식 바이어 전송 API 미확인 | 허용된 실제 브라우저 + 건별 승인 |
| 업무 메일 보내기/받기 | 대기 | 계정·범위 미승인 | 로컬 Mailpit만 개발 대체 |
| OpenAI 호환 AI | 설계만, 기본 비활성 | 화면 등록·승인 후 | 테스트 1건도 비용 승인 전 차단 |
| ChatGPT·Grok 구독 로그인 | 사용 불가 | 본 앱 허가 근거 없음 | fail-closed. 코어와 분리 |
| BrowserOS neo | 대기 | 공식 연결 미제공 | 없으면 수동 CSV |
| Cloudflare 화면 배포 | 설계만 | Workers Static Assets 선택 | 배포 승인 전 생성 안 함 |
| Oracle 워커·새 DB | 설계만 | 구버전 호스트 기록만 참고 | 설치 전 read-only preflight + 배포 승인 |
| 암호화 백업 저장소 | 미생성 | 외부 보관 필요 | 버킷·비용은 배포 승인에 포함 |

## 승인 범위 — 지금 하는 것 / 안 하는 것

지금 허용: M0 문서와 정적 시안. 디자인 승인 후 로컬 애플리케이션 구현. 합성 데이터 검증.

항상 사전 승인: 유료 API, 이메일·업체 연락, Oracle/Cloudflare/R2 배포, 데이터 삭제, 기존 데이터 이관, 공유 호스트 재부팅.

질문 기본안(Q1–Q8)은 설계값이지 위 행위의 승인이 아니다.

## 하지 않는 일

- 자동 결제·자동 발주 실행
- 미확인을 0/합격으로 바꾸기
- 견적 A 단가와 견적 B 최소수량을 섞기
- 개발 PC 워커가 프로덕션 일을 처리
- 구독 로그인을 허가 없이 “연결됨”으로 표시
- 장식용 가짜 제품 사진·수익 확률 타일
