# Design System: Forge Kitchen Ops

## 승인 기록

| 항목 | 값 |
|---|---|
| 상태 | **Jay 승인** |
| 작성 | 2026-09-06 |
| 시안 | `design/screens.html` (정적 검토용. 애플리케이션 아님) |
| 승인된 문서 hash | `f4e17fadeee52bcddc1a96fa164866c3b735e1c69a182135fb9bab3725cec9c5` (승인 직전 DESIGN.md SHA-256) |
| 시안 hash | `5fc1f65c84ed9361a6fa4c6f0069b05ec62494aa5e601c58eb55ef2fbc7fe33f` |
| 승인 시각 | 2026-09-06T03:13:14-05:00 |

이 색·글꼴·간격은 승인된 체계다. 구현은 `packages/ui` 토큰으로만 맞춘다. 유료 이미지 생성은 쓰지 않았다.

구현 후 같은 네 화면을 375/768/1280에서 찍어 이 문서와 대조한다. 기능 합격이 시각 합격을 대신하지 않는다.

---

## 1. 분위기

이름: **오늘 처리할 승인함**.

Jay는 개발자가 아니다. 아침 몇 분 들어와 다음에 누를 일과 그 근거를 같은 화면에서 읽는다. 작업대 위 칼과 도마처럼 도구가 제자리에 있다. 성장 대시보드가 아니다.

- 밀도: 일상 앱 (5/10). 한 결정에 숨 쉴 여백. 숫자는 견적 비교에서만 촘촘하다.
- 변화: 예측 가능 (3/10). 버튼 위치는 매일 같다.
- 움직임: 절제 (2/10). 120–180ms, 상태 알림에만. `prefers-reduced-motion`이면 0.

주방용품·스테인리스·도마 종이의 밝은 작업 공간. 어두운 테크 콘솔, 거대한 KPI, 수익 확률, 서버 로그는 첫 화면에 두지 않는다. 기본은 밝은 화면만. OS 다크 모드로 업무 화면을 뒤집지 않는다(운영 실수 방지).

## 2. 색

한 행동색. 상태색은 행동색과 섞지 않는다. 상태는 색 + 문구 + 아이콘.

| 이름 | Hex | 역할 | 본문 대비율 |
|---|---|---|---|
| Canvas | `#F4F6F8` | 페이지 배경 | — |
| Ink | `#172B3A` | 본문 | Canvas 13.43:1, Surface 14.55:1 |
| Surface | `#FFFFFF` | 목록·패널 | — |
| Muted | `#3E5160` | 보조 문장 | Canvas 7.60:1 |
| Line | `#D5DCE3` | 1px 구분 | 텍스트 아님 |
| Action | `#164E63` | 주 버튼, 링크, focus | Surface 9.11:1, 흰 글자 9.11:1 |
| Action pressed | `#0F3A4A` | active | 흰 글자 충분 |
| Warn ink / wash | `#854D0E` / `#FEF3C7` | 주의, 추정 | 6.15:1 |
| Danger ink / wash | `#991B1B` / `#FEE2E2` | 거절, 위험 | 6.80:1 |
| Success ink / wash | `#166534` / `#DCFCE7` | 통과, GO | 6.49:1 |

근거 칩:

- 측정 — 성공 워시 + “측정”
- 추정 — 경고 워시 + “추정”
- 견적 — 흰 바탕, Action 테두리 + “견적”
- 미확인 — 점선 Line, Muted 글자 + “미확인”. 초록 금지.

판정: GO = 성공, CAUTION = 경고, 보류 = Muted 점선, 탈락 = 위험. 판정은 단계 이름이 아니다.

Focus: 2px solid Action, offset 2px. 배경이 Action이면 흰 링.

금지: `#000000`, 보라/네온, 그라데이션 글자, 외곽 글로우, 장식 그라데이션 배경.

## 3. 글꼴

자체 호스팅 **Noto Sans KR** variable + system sans.

```
"Noto Sans KR", "Malgun Gothic", "Apple SD Gothic Neo",
"Noto Sans", ui-sans-serif, system-ui, sans-serif
```

시안 HTML은 외부 폰트 CDN을 쓰지 않는다. 검토 PC에 Noto가 없으면 Malgun/Apple SD로 읽는다. 앱 구현 때 폰트 파일을 `packages/ui`에 넣고 빌드한다.

| 역할 | 크기 / 행간 / 두께 |
|---|---|
| 페이지 제목 | 32px / 1.25 / 650 |
| 화면 제목 | 24px / 1.30 / 650 |
| 본문 | 16px / 1.50 / 400 |
| 보조 | 14px / 1.45 / 500 — 최소 14px |
| 숫자 | 본문과 동일, `font-variant-numeric: tabular-nums` |

설명 문단 폭 ≤ 65ch. 견적 비교는 표/스택이지 수필이 아니다. 해시·SQL·endpoint를 화면에 쓰지 않으므로 별도 모노 글꼴은 업무 UI에 두지 않는다.

## 4. 간격·형태

- 간격 토큰: 4 / 8 / 12 / 16 / 24 / 32px. 요소 마진이 아니라 그룹 `gap`.
- 모서리 8px. 더 둥글게 만들지 않는다.
- 입력·주 버튼 최소 높이 44px, 좌우 패딩 ≥ 16px.
- 카드는 한 겹. 카드 안의 카드 금지.
- 가로로 돌아가는 카드 금지. 페이지 가로 스크롤 금지. 넓은 표만 칸 안에서 가로 스크롤.
- 터치 목표 44px. 아이콘만 있는 제어는 금지 — 항상 글자.

## 5. 움직임

허용: 화면 전환 opacity/transform 120–180ms, 승인 직후 같은 카드에 “승인 기록됨” 1회.

금지: 무한 펄스, 타이핑 효과, 패럴랙스, 숫자 롤링, 축하 컨페티.

`prefers-reduced-motion: reduce`이면 전환 0. 상태 문구는 남긴다.

## 6. 경로와 폭

| 경로 | 운영자 이름 |
|---|---|
| `/` | 오늘 |
| `/research` | 가져오기 |
| `/candidates` | 후보 |
| `/candidates/:id` | 후보 상세 |
| `/sourcing` | 견적 |
| `/settings` | 설정 |
| `/security` | 보안 (설정에서 들어감) |

연결은 “정글스카웃 API · 연결됨/대기”처럼 이름과 상태만. endpoint 문자열 없음.

### 1280px

왼쪽 고정 216px 사이드바(이름 Forge Kitchen Ops, 오늘/후보/가져오기/견적/설정). 가운데 목록. 오늘 화면은 오른쪽 약 360px 근거 패널 — 목록에서 고른 한 건. 후보 상세·견적·설정은 목록+패널 대신 한 칼럼 본문(max 960px).

### 768px

사이드바 없음. 상단 가로 탐색. 목록만. 상세는 별도 화면.

### 375px

하단 4탭: 오늘 / 후보 / 견적 / 설정. 가져오기·보안은 설정 또는 오늘 빈 화면 CTA. 승인 카드 한 열. 상세는 근거 → 모르는 것 → 다음 행동 순. 견적은 견적별로 같은 필드를 세로 반복. 페이지 가로 스크롤 없음.

## 7. 후보 문법 (모든 목록의 유일한 요약)

```
CandidateView = {
  id, keyword, stage,
  evidenceSummary,   // 사람이 읽는 짧은 근거
  unknowns,          // 문자열 목록. 없으면 “지금 모르는 것은 없습니다”
  nextAction: { kind: 'automatic' | 'approval' | 'waiting', label, target }
}
```

`nextAction`은 한 개. 승인 필요면 그 버튼만 크게. 자동이면 “자동 확인 중”이고 승인 버튼 없음. 대기면 “웹 연결을 기다리고 있어요”처럼 이유와 다음에 할 일만.

단계 라벨(한국어 / 내부키는 화면 숨김):

| 화면 | 내부 |
|---|---|
| 가져옴 | imported |
| 선별 중 | screening |
| 공식 확인 중 | api_validation |
| 공급처 찾는 중 | sourcing |
| 견적 초안 | rfq_draft |
| 연락 승인 대기 | awaiting_contact_approval |
| 견적 요청 보내는 중 | sending_rfq |
| 견적 대기 | awaiting_quote |
| 손익 검토 | economics_review |
| 발주 판단 대기 | awaiting_order_decision |
| 결정 기록됨 | decision_recorded |
| 탈락 | rejected |

막힘 이유(`blockedReason`)는 단계와 별도: 웹 연결 / 예산 / 로그인 정보 / 근거 / 외부 결과 확인 중 / 연결 없음.

GO/CAUTION/보류는 평가 문장이지 단계가 아니다. GO여도 “수익성 확인”이라 쓰지 않는다. “지금 입력 기준 충족 · 견적 기반”처럼 근거 종류를 붙인다.

## 8. 화면별 시안

대표 시안 네 장 + 오늘 빈 화면. 파일 `design/screens.html`. 뷰포트 375/768/1280과 한국어/영어를 시안 도구로 전환한다.

### 8.1 오늘 `/`

제목: 오늘 처리할 일. 한 줄 요약: `진행 17 · 승인 대기 2`.

승인 카드 구조(위→아래): 키워드, 단계, 짧은 근거, 모르는 것, 주 버튼 하나.

시안 데이터(합성, 실제 업체가 아님):

1. `silicone spatula set` — 연락 승인 대기. 버튼 “견적 요청 2건 보내기”.
2. `garlic press` — 발주 판단 대기. 견적 A 기준 GO(견적 기반). 버튼 “발주 판단 보기”.

기다림 목록(버튼 없음): `bamboo cutting board` — 웹 연결을 기다리고 있어요.

자동 요약: 나머지 후보는 자동 확인 중입니다.

빈 승인: 제목 아래 “지금 승인할 일이 없어요.” + 자동 진행 한 줄.

첫 실행(업무 0): “아직 가져온 제품이 없어요.” 주 버튼 “제품 가져오기”.

금지: 상단 4칸 KPI, 원형 차트, “성공 확률 78%”.

### 8.2 후보 상세 `/candidates/:id`

순서 고정: **다음 행동 → 모르는 것 → 근거**. 그 다음 경쟁 규칙, 공급처, 계산.

시안 키워드 `silicone spatula set`. 이미지 자리: 회색 면 + “이미지 없음” (실제 출처 없는 사진 금지).

근거 예: 1위 가격 $24.99 측정, 월 매출 구간 추정, 리뷰 장벽 통과 측정. 반품률·Amazon 브랜드 = 미확인.

주 버튼만: “견적 요청 2건 보내기”. 거절은 보조.

### 8.3 견적 `/sourcing`

같은 사양 revision. 견적 A/B를 섞지 않는다.

데스크톱: 두 열, 같은 필드 행이 가로로 맞춰짐.  
모바일: A 전체 필드 후 B 전체 필드. 같은 라벨 반복.

시안 숫자(PLAN 산술, USD):

| | 견적 A | 견적 B |
|---|---|---|
| 수량 | 300 | 600 |
| 도착원가 | 6.00 | 5.00 |
| 광고 전 이익 / 마진 | 14.00 / 46.67% | 15.00 / 50.00% |
| 광고 후 이익 / 마진 | 12.00 / 40.00% | 13.00 / 43.33% |
| ROI | 200.00% | 260.00% |
| 출시 현금 | 2,600 | 3,800 |
| 판정 | GO · 견적 기반 | CAUTION · 출시 현금 한도 초과 |

판매가 30, FBA 4.50, referral 4.50, 광고 2, 반품 0.50, 기타 0.50. A 선지급 200, 초기광고현금 300, 예비 300.

운임 미확인이면 그 견적은 “계산할 수 없음 · 보류”. 견적 0건은 탈락이 아니라 “견적 대기”.

### 8.4 설정 `/settings`

그룹: 판매 기준, 출시 현금, 니치 규칙, 연결, 언어·알림.

값 옆 “지금 적용 중” 버전. 바꾸면 저장이 아니라 “변경 승인 요청”. 전/후 한 줄. 미승인 입력은 적용되지 않음.

연결 행 예: `정글스카웃 웹 · 대기`, `정글스카웃 확인(API) · 한도 없음(호출 안 함)`, `업무 메일 · 대기`, `ChatGPT 구독 · 사용 불가`.

비밀 입력: 쓰기 전용, 표시는 “설정됨 ·•••A9K2” 형태. 키 전체 없음.

언어: 한국어 / English. USD, US, ROI는 그대로.

## 9. 상태 문구

| 상태 | 한국어 | English |
|---|---|---|
| 첫 실행 | 아직 가져온 제품이 없어요 | No products imported yet |
| 승인 0 | 지금 승인할 일이 없어요 | Nothing to approve right now |
| 로딩 | 불러오는 중 | Loading |
| 실패 | 목록을 불러오지 못했어요. 다시 시도해 주세요 | Couldn’t load this list. Try again |
| 오프라인 | 연결이 끊겼어요. 이미 열린 내용은 그대로입니다 | You’re offline. What’s already open is still here |
| 오래된 평가 | 기준이 바뀌어 이 계산은 오래되었습니다 | Criteria changed. This calculation is out of date |
| 견적 없음 | 아직 견적이 없어요. 탈락이 아닙니다 | No quotes yet. That isn’t a rejection |
| 웹 대기 | 웹 연결을 기다리고 있어요 | Waiting for the signed-in browser |
| 전송 불명 | 보냈는지 아직 확인 중이에요. 다시 보내지 않습니다 | Send result unknown. It will not be resent |
| 미확인 | 미확인 | Unknown |

버튼:

| 한국어 | English |
|---|---|
| 제품 가져오기 | Import products |
| 견적 요청 2건 보내기 | Send 2 quote requests |
| 발주 판단 보기 | Review order decision |
| 변경 승인 요청 | Request approval to change |
| 거절 | Reject |

## 10. 부품

- **주 버튼**: Action 채움, 흰 글자, 높이 44, 모서리 8. active 시 1px 아래.
- **보조 버튼**: 흰 면, Line 테두리, Ink 글자.
- **위험 보조**: 위험 글자, 채움 없음.
- **승인 카드**: Surface, Line 1px, 패딩 16, gap 12. 한 겹.
- **칩**: 높이 24 이상, 좌우 8, 14px. 아이콘 12–14px + 글자.
- **입력**: 라벨 위, 오류 아래. 플로팅 라벨 없음.
- **빈 화면**: 제목 + 한 문장 + 있을 때만 버튼. 일러스트 남용 없음.
- **로딩**: 해당 목록 높이의 회색 막대. 원형 스피너 남용 없음.

SVG 아이콘은 상태 중복 표시용. 이모지로 상태를 대체하지 않는다.

## 11. 접근성

- WCAG AA: 본문 4.5:1, 큰 글자·UI 3:1. 위 토큰은 본문 기준 통과.
- 키보드만으로 오늘 → 승인 버튼. focus 항상 보임.
- 폼 라벨은 `label`로 연결.
- 상태 알림은 문구가 있고, 색만으로 의미 전달 금지.
- 200% 확대 시 가로 스크롤로 필수 버튼을 잃지 않음.
- 스크린리더 이름: 버튼은 동작 전체 (“견적 요청 2건 보내기”).

## 12. F11 검수표 (구현 후 실측)

| 검사 | 시안 | 앱 375 | 앱 768 | 앱 1280 | 이탈 |
|---|---|---|---|---|---|
| 글꼴·16/1.5 | | | | | |
| 간격 4–32, 모서리 8 | | | | | |
| Ink/Canvas/Action | | | | | |
| 버튼·입력 높이 44 | | | | | |
| 오늘: KPI 없음, 행동 1개 | | | | | |
| 상세: 행동→모름→근거 | | | | | |
| 견적: 모바일 세로 반복, 가로 스크롤 없음 | | | | | |
| 이미지 없음 처리 | | | | | |
| 한국어 긴 키워드 / 영어 | | | | | |
| 빈/오류/미확인 문구 | | | | | |

제품 사진은 출처·사용권이 있을 때만. 없으면 “이미지 없음”.

## 13. 하지 말 것

- Inter, 제네릭 세리프, 순수 검정
- nested card, 가로 카드 회전, 3열 동일 메트릭 카드
- 가짜 제품 사진, 99.99%, 성공 확률
- Elevate/Seamless 카피, 이모지 섹션 마커
- 페이지별 임시 스타일. 수정은 `packages/ui` 토큰으로만
- 이전 프로젝트 화면을 열고 베끼기

## 14. 시안 범위 밖 (앱이 나중에 구현)

로그인·2FA, 가져오기 미리보기, 보안 기기 목록, 실제 승인 API. 시안에서 주 버튼을 눌러도 메일이 나가지 않는다. 경계 문구: “이 시안은 보내지 않습니다.”
